<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PizzaApp.View.HomeView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Pizza App" HorizontalOptions="Center" VerticalOptions="Center" VerticalTextAlignment="Center" Margin="10"/>
            <Grid Margin="10,30,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="200"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Text="Pizzas" Command="{Binding PizzaCommand}" Grid.Row="0" Grid.Column="0" ImageSource=""/>
                <Button Text="Cart" Grid.Row="0" Grid.Column="1" Margin="1,1,1,80" Command="{Binding CartCommand}"/>
                <Button Text="Coupons" Grid.Row="1" Grid.Column="0" Margin="1,-10,1,-50" Command="{Binding CouponsCommand}"/>
                <Button Text="Orders" Command="{Binding OrdersCommand}" Grid.Row="1" Grid.Column="1" Margin="1,-90,1,-50"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>